{
  "name": "concerto",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/tenant-site",
    "build": "npm run build --workspace=apps/tenant-site",
    "dev:admin": "npm run dev --workspace=apps/admin-dashboard",
    "build:admin": "npm run build --workspace=apps/admin-dashboard"
  }
}
